# QA Report — v16/07/2026 (Gộp role Supervisor + 8 bug UI + Work/Demo)

**Changelog kiểm:** Obsidian `Nhật ký phát triển.md` → "Phiên 16/07"
**Người chạy:** Fable 5 (fix) — theo feedback màn hình của chủ dự án
**Ngày:** 16/07/2026

---

## L1 — Automated
| Check | Kết quả |
|---|---|
| pytest full | **308 passed / 0 failed** |
| tsc --noEmit | **clean** (exit 0) |
| next build (production) | **OK — 21 trang compile** |
| RBAC matrix (6 role) | pass (đã gộp còn admin/executive/leader/data_entry/accountant/supervisor) |
| Zalo tests | 10/10 pass |

## Bug feedback đã xử lý

| # | Feedback | Nguyên nhân | Fix |
|---|---|---|---|
| — | Tài khoản Thiết kế/Thu mua/PM không vào được + supervisor demo lỗi | Frontend còn key role cũ (ROLE_DEPT, ROLE_TOURS, OnboardingChecklist, HR dropdown) sau khi backend đã gộp supervisor | Dọn sạch designer/pm/purchasing → supervisor ở roles.ts, hr, GuidedTour, OnboardingChecklist, page.tsx; backend projects.py 2 guard `pm`→`supervisor` |
| — | Login Work vẫn ra Demo | `api.login` kiểm `isDemoMode()` (cờ `jama_demo`) TRƯỚC khi login xóa cờ | auth.tsx: xóa `jama_demo=false` + set mode work TRƯỚC khi gọi api.login |
| 1 | Toggle trạng thái không bấm được (`/users`) | Demo data tĩnh + nuốt lỗi im lặng | Optimistic update + hiện lỗi khi thất bại |
| 2 | Sidebar nên ≥10 mục desktop rồi mới "Xem thêm" | ROLE_ESSENTIALS giới hạn 5 | Desktop hiện tối đa 10 mục (role-curated đầu) |
| 3 | "Xem lại hướng dẫn" chỉ nhảy về Tổng quan | Sidebar+GuidedTour render trong TỪNG page → router.push unmount → tour mất | Lưu trạng thái tour vào localStorage, instance mới tự khôi phục card |
| 4 | AI Model không cấu hình được | `if (!data) return null` → section biến mất khi getAISettings lỗi/demo | Demo resolver /ai-settings + fallback data (không return null) |
| 5 | Vào trang trong "Xem thêm" thì sidebar co lại | Trang active không được giữ trong danh sách hiện | Promote trang đang mở (pathname) vào visible set |
| 6 | Logo login nhỏ | h-16 | h-24 sm:h-28 max-w-280 |
| 7 | Demo supervisor lỗi | = root cause dọn role | (đã fix ở trên) |
| 8 | P&L executive chỉ xem, không click dự án | Row không có onClick | Thêm modal chi tiết P&L dự án (doanh thu/chi phí/lợi nhuận/biên + phân tích chi phí + nút mở dự án) |

## L3 — Bất biến (giữ nguyên)
6 role RBAC khớp frontend↔backend; kỳ lương khóa 409; user inactive chặn login; demo không gọi API thật; audit log; UI thuần Việt.

## Verdict: **✅ PASS** — 308 test + build OK. Deploy Railway + Vercel.

### Chưa làm (follow-up)
- Settings UX "khoa học hơn": gom nhóm section (Cá nhân / Tích hợp / Hệ thống) — polish, chưa gấp.
- Zalo Ingest Service cần chạy trên VPS + npm install zca-js để test QR runtime.
