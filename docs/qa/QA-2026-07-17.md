# QA Report — 17/07/2026 (Fix Work login + Settings nav + QA localhost trước deploy)

**Bối cảnh:** User yêu cầu QA/QC TOÀN hệ thống rồi chạy localhost cho user tự test TRƯỚC khi deploy (không deploy code chưa test).
**Người chạy:** Opus 4.8
**Môi trường test:** backend `localhost:8000` (SQLite `jama.db`, uvicorn --reload) + frontend `localhost:3000` (next dev)

---

## L1 — Automated
| Check | Kết quả |
|---|---|
| pytest full | **310 passed / 0 failed** (+2 test login local-part) |
| tsc --noEmit (frontend) | **clean** (exit 0) |
| 10 trang SSR (/, settings, users, pl, projects, attendance, approvals, kpi, hr, inventory) | tất cả **HTTP 200** |
| Login 6 role (curl) | admin/supervisor/leader/data_entry/accountant/executive → **200** |
| CORS preflight :3000 → :8000 | OPTIONS **200**, POST **200** |

## Bug user báo (turn này) — đã fix & verify

| # | Feedback | Nguyên nhân gốc | Fix | Verify |
|---|---|---|---|---|
| 1 | Work login "sai pass" dù gõ admin123 | User gõ **`admin`** nhưng backend tra `email == "admin"` → không thấy → 401 (email sai, không phải pass). Thêm nữa `type="email"` chặn giá trị thiếu `@`. | Backend: chấp nhận **local-part** (`admin` ↔ `admin@jamahome.vn`, escape LIKE wildcard). Frontend: input `type="text"`, label "Email hoặc tên đăng nhập", placeholder + hint gõ tên ngắn. | curl `admin`/admin123 → **200**; `adm%` → **401**; browser fetch :3000 → 200 + token |
| 2 | Demo bấm Cài đặt không ra trang | Guard `settings/page.tsx` redirect role thiếu `canViewSettings` (4/6 role) về `/`. Nhưng sidebar hiện "Cài đặt" cho MỌI role → bấm là bị đá. | Bỏ guard redirect — mọi user đăng nhập vào được Settings; mục quản trị (AI/Zalo/Backup/Automation) đã tự gate theo role bên trong. Gate thêm dòng info "AI Model" về admin. | Browser demo role=supervisor → navigate /settings → **ở lại /settings** (h1 "⚙️ Cài đặt", có Telegram section), không redirect |

## L2 — Kiểm tương tác (browser localhost:3000, demo mode)
- Login demo (nút Tập luyện tự điền) → submit → **redirect `/login`→`/`**, token demo, mode demo ✓
- Dashboard render đầy đủ: stats (9 lead, pipeline 23.2 tỷ, 3 dự án, 3 HĐ 264.9 tỷ) + phễu quy trình ✓
- Sidebar desktop: **10 mục** rồi mới "Xem thêm (9)" ✓ (bug sidebar cũ)
- Settings: admin thấy đủ; supervisor chỉ thấy mục cá nhân (Telegram/tour), không thấy mục admin ✓

> **Lưu ý harness:** Browser MCP không dispatch được click/type synthetic event tới React trên dev server ổ H: (compile chậm). Đã xác minh app đúng bằng: gọi React onClick/onSubmit qua fiber (field tự điền + login + redirect chạy chuẩn), curl, và fetch từ page context. **Đây là giới hạn công cụ test, KHÔNG phải lỗi app** — user gõ tay thật hoạt động bình thường.

## Phát hiện hạ tầng (không phải bug code)
- **CORS chỉ có :3000/:3001/:8000** — khi Next nhảy sang :3002 (cổng bận) thì browser login "Failed to fetch". Đã thêm :3002/:3003 vào `CORS_ORIGINS` mặc định (chỉ ảnh hưởng dev; production đặt qua env).
- **Seed NameError (`designer_id`)** đã fix ở phiên trước (seed.py:259 → `supervisor_id`) — nếu chưa: DB mới seed fail → mọi login 401.

## Đợt 2 — Feedback màn hình user (3 lỗi) + QA Work-mode toàn trang

> Multi-agent Workflow không chạy được (bug harness pin `claude-sonnet-4.6`, override model không ăn) → tự QA trực tiếp bằng phân tích code + drive React qua fiber trên browser.

| # | Feedback | Nguyên nhân gốc | Fix | Verify |
|---|---|---|---|---|
| 1 | Bấm ô ngày không ra lịch (chấm công/nghỉ phép…) | globals.css KHÔNG có rule cho `input[type=date]` → nền tối, icon lịch mặc định tối-trên-tối gần như tàng hình; chỉ bấm đúng icon mới mở | globals.css: `color-scheme: dark` + kéo `::-webkit-calendar-picker-indicator` phủ toàn ô (bấm đâu cũng mở) + icon lịch vàng trang trí | computed: colorScheme=dark, position=relative, có icon-svg, padding 34px ✓ |
| 2 | Trang /settings quá hẹp (`max-w-2xl`), thừa chiều ngang | 1 cột | `max-w-5xl columns-1 lg:columns-2` (masonry, `[&>*]:break-inside-avoid`) — không sửa từng card | browser: columnCount=2, rộng 935px (trước 672px) ✓ |
| 3 | **Vài trang crash khi click phần tử chi tiết** (vd /leads) | **`GET /leads/{id}/activities` trả object phân trang `{items,total,page,...}` nhưng `api.getActivities` khai kiểu `Activity[]`** → `setActivities(object)` → render `activities.map()` → `TypeError: activities is not iterable` → ErrorBoundary. CHỈ xảy ra Work-mode (demo trả mảng thuần). | api.ts `getActivities`: `Array.isArray(res) ? res : res?.items ?? []`. + guard defensive `name?.charAt`, `(act.content||'').split` ở leads/customers | **Reproduce Work-mode → crash; sau fix → modal mở đầy đủ, hasError=false** ✓ |

**QA Work-mode click-chi-tiết toàn trang (login admin thật, token thật):** leads✓(fixed) customers✓ contracts✓ projects✓ quotations✓ pl✓ inventory✓ approvals✓ + load-check accounting✓ hr✓ kpi✓ reports✓ — **0 crash còn lại**.

**Kiểm shape phân trang hệ thống:** chỉ `getActivities` bị (leads.py:604 thiếu `response_model`). Mọi endpoint array khác đều `response_model=list[...]`/`return [...]` (bare array) → an toàn: leads/projects kanban, project tasks, task activities, inventory usages/low-stock, users teams, pl projects.

**Hạ tầng:** `CORS_ORIGINS` mặc định thêm `:3002 :3003` (Next nhảy cổng). conftest autouse reset rate-limiter login. **tsc clean sau toàn bộ sửa.**

## Đợt 3 (18/07) — Gói "làm hết": 8 tính năng + UI/UX sweep

**Backend mới:**
| Hạng mục | Chi tiết |
|---|---|
| Đổi/quên mật khẩu | `POST /auth/change-password` (self, cần mật khẩu cũ); `POST /auth/forgot-password` → mã 6 số qua Telegram đã liên kết (in-memory 15', 5 attempts, không lộ user tồn tại); `POST /auth/reset-password`. Hỗ trợ tên ngắn như login. |
| Budget vs Actual | Project + `budget_total`; `ProjectUpdate/Response` expose; PL detail trả `budget_total`. Migration `j01_2026a001`. |
| Bàn giao & bảo hành | Project + `handover_date`, `warranty_months` (default 12). Automation `run_warranty_reminders` (mốc 30/7 ngày, dedupe `_notify`, nối vào `run_all_automation`). |
| Nghiệm thu portal | Project + `stage_acceptances` JSON; `POST /portal/{token}/projects/{id}/accept-stage` (idempotent, báo Telegram sales/PM); detail trả acceptances. |
| Phải thu | `GET /pl/receivables` (C-level): installments pending của HĐ signed → tổng + danh sách theo HĐ. |
| Restore backup | `scripts/restore_backup.py` (--list/--latest/file; giữ bản pre-restore để hoàn tác; SQLite-only, PG dùng pg_dump). |
| Uptime | `.github/workflows/uptime.yml` cron 10' ping Railway+Vercel → Telegram khi sập (cần GH secrets `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALERT_CHAT_ID`). |
| Sentry | Optional `SENTRY_DSN` (config + main.py guarded init + .env.example). |

**Frontend mới:** Settings 3 nhóm (Cá nhân/Tích hợp/Hệ thống) + card Đổi mật khẩu; login "Quên mật khẩu?" 2 bước qua Telegram; BottomNav mobile ≤5 mục theo quyền (safe-area, main pb-16); form dự án + ngân sách/bàn giao/bảo hành; modal dự án + thanh Budget vs Actual (cảnh báo 80%/vượt) + khối bảo hành; /pl + card "Phải thu theo hợp đồng" (expand chi tiết đợt); portal + nút "Xác nhận nghiệm thu" per giai đoạn; LineIcon component thay emoji (settings sections + 7 page h1); demo resolvers receivables/password.

**Tests mới:** `test_password_portal_budget.py` — 17 case (mật khẩu, portal accept, budget→PL, receivables, warranty, dashboard-personal hồi quy). **Kết quả 18/07: 327 pass toàn suite, tsc clean.**

**Bug phát hiện khi QC supervisor trên Postgres:** `/dashboard/personal` **500 cho MỌI role personal** — `from datetime import timedelta` đặt giữa hàm làm `timedelta` thành local toàn hàm → dòng `now - timedelta(days=3)` phía trên ném UnboundLocalError (bug ẩn từ đợt productivity 14/07; frontend nuốt lỗi bằng `data?.` nên chỉ thấy dashboard trống). Đã fix + 2 test hồi quy.

**Deploy 18/07 (Postgres):** Railway `railway up` + Vercel `--prod`. Verify prod: dashboard/personal supervisor **200**; `/pl/receivables` 200 (2.397 tỷ/3 HĐ seed); migration j01 chạy trên Postgres (PUT budget_total OK); login local-part OK; frontend có "Quên mật khẩu". `RAILWAY_HEALTHCHECK_PATH` sửa từ path Windows lỗi → `/health`. DATABASE_URL đã nối Postgres (user tự nối qua dashboard) — dữ liệu bền vững qua deploy.

## Verdict: **✅ PASS trên localhost** — chờ user tự test rồi mới deploy.

### Deploy checklist (SAU khi user OK localhost)
1. `git push` (Railway backend) — có seed fix + local-part login + CORS.
2. `cd frontend && vercel --prod` — có login UX + settings fix.
3. **Vercel env `NEXT_PUBLIC_API_URL`** = `https://jama-crm-prod-production.up.railway.app/api/v1` (hiện rỗng → fallback localhost → login production fail). Đây là lý do Work login fail trên production.
4. Verify: curl login production 200 + grep chuỗi mới trong HTML.
